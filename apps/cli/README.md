# EnvVault CLI

Encrypted environment variable sync for small development teams.

```bash
npx @itspawansaini/envvault login
npx @itspawansaini/envvault init
npx @itspawansaini/envvault pull
```

The CLI encrypts and decrypts project values locally. The EnvVault API receives ciphertext only.
